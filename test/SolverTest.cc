#include "Solver.hpp"

#include <catch/catch.hpp>

using namespace vrp::algorithms;
using namespace vrp::algorithms::construction;

namespace vrp::test {

SCENARIO("Solver can solve simple problem with default algorithm", "[solver][default]") {
  GIVEN("Solver with default algorithm with cheapest insertion") { /*auto solver = Solver<DefaultAlgorithm<>>{};*/
  }
}
}
